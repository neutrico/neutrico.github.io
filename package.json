{
  "scripts": {
    "preversion": "jekyll server --detach && node-sass _styles/main.scss | postcss -c postcss.json && uglifyjs _scripts/main.json -o assets/scripts/main.js",
    "postversion": "pkill -f jekyll"
  },
  "devDependencies": {
    "browser-sync": "^2.13.0",
    "cssnano": "^3.7.1",
    "fs-extra": "^0.30.0",
    "node-sass": "^3.8.0",
    "postcss-cli": "^2.5.2",
    "postcss-uncss": "0.0.1"
  },
  "version": "0.23.0"
}
